import 'package:flutter/material.dart';

void main() {
  runApp(
    p1(),
  );
}

class p1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text("Testeeeee")),
      ),
    );
  }
}
